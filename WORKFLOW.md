# 智能盲杖 Demo v1 — 电控开发工作流程

> 本文档是前期讨论的共识记录 + 开发流程约定。
> 后续所有阶段的产出物、验收标准均以本文为准。任何共识变更请回改本文。
>
> **当前进度：阶段 0 已完成（需求冻结）→ 阶段 1 / 2 产出物已就位（在 `demo/docs/`）。**
> 开放项已于 2026-09-23 逐条确认，见第 6 节。
>
> **远程仓库：<https://github.com/ciolence/SmartStick>（分支 `main`）——日常 Git 操作见第 9 节。**

---

## 1. 项目背景

- 做一个面向视障人士出行辅助的**智能导盲杖**（Demo v1）；功能划分与硬件选型参考同类开源导盲杖项目的公开资料。
- 参考项目为「双树莓派 + 双 micro:bit + ESP32」四机架构，我们**裁剪**为 **STM32F103C8T6 单主控**方案。
- 参考资料已就位：`HAVE.md`（已有模块手册）、`HARDWARE.md`（购置清单 / 参考项目代码实证 / 供电 / 引脚建议 / 踩坑清单）。

## 2. 功能冻结清单（Demo v1）

> 优先级定义：**P0 = v1 必须做**；**P1 = v1 做，但可降级**；**P2 = v1 只预留（引脚/接口/代码桩），不实现业务**。
> 「状态」列记录开放项确认结果（O1~O8）。

### 2.1 要做的

| # | 功能 | 实现硬件 | 优先级 | 说明 |
|---|------|----------|--------|------|
| 1 | **手扶（自动牵引）** | L298N + 双 GA25-370 DC12V 减速电机 + 从动轮（三轮车底盘） | P0 | 双电机差速实现转向与引导；底盘立一根扶杆，盲人手扶跟随 |
| 2 | **避障** | 超声波模块 ×1（正前方，US-100 类 UART 模式） | P0 | 正前方测距 → 分级减速/停车 + 提示 |
| 3 | **姿态检测** | MPU6050（I²C） | P0 | 跌倒/倾角检测，替代参考项目 micro:bit |
| 4 | **报警** | 有源蜂鸣器 + 振动马达（三极管驱动） | P0 | v1 仅**本地报警**（声/震）；跌倒 SOS 与避障提示均走此通道 |
| 5 | **OLED 状态显示** | 已有的 4 脚 I²C OLED | P0 | 现场显示距离/姿态/电量/错误码，是排错利器（O3：并入） |
| 6 | **电池电压监测** | ADC + 电阻分压 | P1 | 低电量报警并入功能 4，防"走到半路没电"（O4：并入，中等优先级） |

### 2.2 v1 只预留、不实现业务（P2）

| # | 项 | 预留方式 | 说明 |
|---|----|----------|------|
| 7 | **蓝牙 UART** | USART3（PB10/PB11）引脚与驱动骨架就位 | O6：**中高优先级**，v1 只预留串口透传 + shell 骨架，不写手机业务；型号 HC-05/JDY-31 类透传，后定 |
| 8 | **QMC5883L 磁力计** | 挂 I²C1 总线（不占新引脚），驱动 + 校准命令就位 | O5：**中等优先级**，v1 应物理安装；只做「能读到数据 + 可校准」，不做航向业务 |
| 9 | **按键** | PA11（SOS）/ PA12（模式）；PB4/PB5 作为备用位 | O2：按键**可预留**，先分配好主要引脚；SOS 键若 v1 装机则按 P1 处理，否则只留驱动 |
| 10 | **超声波第二路 / 舵机云台** | PA0/PA1（第二路 Trig/Echo）；PA8（舵机信号，走软件 PWM） | O1：第二路暂不装；O7：转向绕行代码写好但**默认关闭**，等第二路到位再启用 |
| 11 | **4G/WiFi 联网、GPS、语音** | 不预留引脚（USART3 若被蓝牙占用则需重新分配） | v1 范围外，本阶段不设计 |

### 2.3 明确不做的（v1 范围外）

- 联网上报 / 4G / WiFi（**预留蓝牙 UART 与手机通信**，手机侧另做软件，后期再启动）
- GPS、语音播报（DFPlayer）、语音识别、BME280、视觉（YOLOv5/车牌）、云服务器
- SG90 舵机（参考项目也未使用；引脚可预留，见 2.2 #10）

## 3. 已达成的共识与决策记录

| 主题 | 决策 |
|------|------|
| 核心板 | STM32F103C8T6（64KB Flash / 20KB RAM，资源紧张是既定约束，编码需注意体积） |
| 开发环境 | CubeMX + Keil MDK5；用户负责 CubeMX 图形化配置，代码放入 `demo/` |
| 调试硬件 | 有 ST-Link 可烧录（SWD 在线调试未接触过，可作为兜底手段，不作为主要排错手段） |
| 工程方法 | 认可：串口调试命令行、模块自测入口 + 上电验证清单、CubeMX 共存规范、错误码/日志规范、参数标定机制 |
| 工程方法 | 否决：PC 端模拟验证（嫌麻烦，不做） |
| 报警路线 | v1 纯本地（声/震）；蓝牙→手机上报为 v2 预留（只留串口，不写业务） |
| 交付目标 | 代码力求一次写对（用户无法即时调试，最终统一上硬件调试），因此**便捷调试是一等需求** |
| **目录结构** | 阶段 1/2 的文档放 `demo/docs/`；CubeMX 工程 + 我们写的全部代码放 `demo/SmartStick/`（结构由我规划，见 `demo/docs/ARCHITECTURE.md` 第 2 节） |
| **电机 / 驱动（O8）** | **GA25-370 DC12V 减速电机 ×2 + L298N，开环控制**（推翻原 TT 1:48 方案，**供电树随之改为 12V 主电源**，见 `demo/docs/PINOUT.md`） |
| **超声波（O1）** | v1 只装 **1 个（正前方）**；第二路**引脚预留**（PA0/PA1）；优先选 **US-100/AJ-SR04M 类 3.3V 逻辑**模块，可用 UART 模式，也可用 Trig/Echo 模式（驱动两者都支持） |
| **按键（O2）** | 预留。主引脚 PA11（SOS）/ PA12（模式），备用 PB4/PB5；先只做 GPIO + 去抖 + shell 读键，业务后接 |
| **OLED（O3）** | **并入 v1**；用户提供 OLED 驱动代码，我方只做「适配层 + 显示排版」 |
| **电池电压（O4）** | **并入**，中等优先级；占用 PA4（ADC1_IN4）+ 33kΩ/10kΩ 分压 + 100nF |
| **QMC5883L（O5）** | **预留**，中等优先级，**应装机**；只挂 I²C + 驱动可用 + 校准命令，不做航向业务 |
| **蓝牙（O6）** | **中高优先级**，v1 装模块但只做串口透传骨架；占用 USART3（PB10/PB11），型号后定 |
| **牵引行为（O7）** | v1 基本行为 = **直行**；遇障分级：远→减速+提示，近→停车+报警；**转向/绕行代码写好但默认不启用**（等第二路超声波或磁力计），细节到阶段 4 再与用户确认 |
| **硬件知识缺口** | 遇到不确定的硬件/驱动细节（如 OLED 控制器型号、超声波具体型号、电池规格）**先问用户**，不自行假设 |

## 4. 角色分工

| 角色 | 职责 |
|------|------|
| 你 | 确认需求与开放项；按指南配置 CubeMX 并生成 Keil 工程放入 `demo/`；拿到硬件后按验证清单统一调试、反馈串口日志 |
| 我 | 写全部规划/规范/指南文档；写全部电控代码（驱动层 + 业务层 + 调试设施）；交付验证 SOP；调试期远程排错 |

## 5. 开发流程（阶段 0 ~ 6）

### 阶段 0：需求冻结 ✅ 已完成（2026-09-23）
- 产出：本文档（含第 6 节开放项确认结果）。
- 完成标准：开放项逐条确认后，功能与硬件清单冻结。→ **已达成**。

### 阶段 1：硬件资源规划 ✅ 已产出
- 产出两份文档（`demo/docs/`）：
  - `PINOUT.md` — 外设清单与通信协议表、**引脚分配总表**（核对 STM32 复用约束：I²C/UART 引脚组、TIM 通道映射、SWD 占用、PB3/PB4 等 JTAG 引脚处理）、电源树摘要、扩展预留（蓝牙 UART、I²C 空余地址位、空闲 GPIO 排针、舵机）。
  - `CUBEMX_GUIDE.md` — **直白、简要、可照抄**的 CubeMX 逐项配置流程：引脚命名、时钟树（HSE→PLL→72MHz）、每个外设的参数与 NVIC 优先级、工程设置（MDK-ARM V5）。
- 验收标准：你按指南配置全程无歧义、无自由发挥空间。

### 阶段 2：架构与编码规范 ✅ 已产出
- 产出（`demo/docs/`）：
  - `ARCHITECTURE.md` — 分层结构（HAL → 驱动层 → 服务层 → 应用层）、`demo/SmartStick` 目录规划、模块接口约定、任务/数据流、状态机（避障分级、跌倒检测、报警联动、牵引控制）。
  - `CODE_STANDARD.md` — 命名规范、错误码体系、日志分级、**USER CODE 区规则**（我们的代码只在 USER CODE 区挂钩子 + 独立目录，CubeMX 重新生成不吞代码）、参数集中配置头文件规则、自测函数约定。
- 验收标准：你过目认可，后续代码以它为准绳。

### 阶段 3：CubeMX 工程搭建（你做）
- 按 `demo/docs/CUBEMX_GUIDE.md` 配置并生成 Keil 工程到 `demo/SmartStick/`。
- 我审查生成的工程（引脚、时钟、中断优先级核对）后进入阶段 4。
- **交付形态约定**：工程名 `SmartStick`，`Toolchain = MDK-ARM V5`，`Application Structure = Advanced`，生成后不要改动目录结构。

### 阶段 4：代码实现（我做）
- 顺序：基础框架（统一初始化、错误处理、日志、串口 shell）→ 驱动层逐模块（每模块含 `selftest` 与 shell 测试命令）→ 业务层（避障策略、跌倒检测、报警联动、手扶牵引控制）→ 整合。
- 每完成一个模块即交付，附「用 shell 如何验证它」。
- 编码时兼顾 Flash 体积（不上 DMP/u8g2 全家桶，姿态检测用互补滤波/阈值方案，OLED 用轻量驱动）。
- **用户参与点（预定）**：① OLED 驱动代码接收与适配（阶段 4 前半）；② 牵引转向细节确认（阶段 4 后半，O7）。

### 阶段 5：编译与静态验证（我做）
- 尝试 Keil 命令行编译（`UV4 -b`）做到 0 Error / 0 Warning。
- 产出 `demo/docs/VERIFY.md` — **上电验证 SOP**：接线检查 → 点灯 → I²C 扫描（应见 0x3C/0x68/0x0D）→ 逐模块 shell 自测 → 功能联调；每步写明预期现象与失败排查指引。

### 阶段 6：上硬件统一调试（你做 + 我支持）
- 按 `VERIFY.md` 逐项执行；异常时把串口日志发我，远程定位修复，小步迭代。

## 6. 开放项确认结果（2026-09-23 冻结）

| # | 问题 | 确认结果 | 决策影响 |
|---|------|----------|----------|
| O1 | 超声波数量/型号 | **1 个（正前方）**；预留第二路，若无法预留则放弃（低优先级）；型号待定，优先 3.3V 逻辑（US-100 / AJ-SR04M） | 引脚：USART2（PA2/PA3，UART 模式）**或** PA0(Trig)/PA1(Echo)；驱动需同时支持两种模式 |
| O2 | 按键数量 | **可预留**，先分配主要引脚；低优先级 | PA11 = SOS，PA12 = 模式；PB4/PB5 备用 |
| O3 | OLED 并入 v1 | **并入**；**用户提供驱动代码** | 我方只做适配层 + 显示排版；遇到驱动细节先问用户 |
| O4 | 电池电压监测 | **并入**，中等优先级；留一个 ADC 引脚，由我方判断 | PA4 = ADC1_IN4，分压 33kΩ/10kΩ + 100nF |
| O5 | QMC5883L | **预留**，中等优先级，**应装** | 挂 I2C1（0x0D），只做驱动 + 校准，不做航向业务 |
| O6 | 蓝牙模块 | **中高优先级**，可能要装 | USART3（PB10/PB11），透传；shell 经蓝牙可用 |
| O7 | 牵引 v1 行为 | 先直行；**复杂牵引代码可先写好但不启用**，为扩展做准备；细节（转弯等）到时再问 | 状态机 + 差速接口预留转向指令，编译进固件但默认 `disabled`（参数开关） |
| O8 | 电机确认 | **GA25-370 DC12V 减速电机 + L298N，开环**（**变更！**原为 TT 1:48 3~6V） | **供电树改为 12V 主电源**；PWM 频率 18kHz；L298N 压降 ~2V 需计入；电机电流需重新评估（见 `demo/docs/PINOUT.md` 风险项 R1/R2） |

> 未在本次确认、但阶段 4 前必须澄清的细节（**届时问用户，不自行假设**）：
> 1. OLED 控制器是 SSD1306 还是 SH1106（影响列偏移）；是否 128×64。
> 2. 超声波具体到货型号（决定驱动模式选 UART 还是 Trig/Echo）。
> 3. 12V 电源的具体形态（3S 锂电组 / 12V 铅酸 / 12V 电源适配器），以及整机对重量/续航的要求。
> 4. 第二路超声波是否最终安装（决定转向绕行是否启用）。

## 7. 调试策略（为什么能"一次写对"）

1. **串口 shell**：电脑发文本命令即可单独驱动任意执行器、读任意传感器、改参数、跑校准——把"在线调试"降级为"在线人肉测试"，不依赖 SWD 技能。
2. **模块自测入口**：每个驱动自带 `selftest`，配合 shell 逐模块验证。
3. **验证 SOP**：`VERIFY.md` 让统一调试变成按单执行，每步有预期现象和排查指引。
4. **错误码 + 日志分级**：故障自报家门，而非"没反应"。
5. **参数集中 + shell 在线调参**：阈值、标定值不发版即可修改。
6. **CubeMX 共存规范**：保证调试期反复改配置、重新生成代码不会弄丢我们的代码。
7. 兜底：ST-Link 在线调试（后续可补一份 SWD 快速上手说明）。

## 8. 交付物清单与目录结构

```
smart_sti/                          ← Git 仓库根（远程：github.com/ciolence/SmartStick）
├── README.md                       （仓库首页简介）
├── .gitignore                      （编译产物 / 临时文件排除规则）
├── HARDWARE.md / HAVE.md           （前期资料，不动）
├── WORKFLOW.md                     （本文：总纲 / 进度追踪）
└── demo/
    ├── docs/                       ← 我产出的全部文档
    │   ├── PINOUT.md               （阶段 1）
    │   ├── CUBEMX_GUIDE.md         （阶段 1）
    │   ├── ARCHITECTURE.md         （阶段 2）
    │   ├── CODE_STANDARD.md        （阶段 2）
    │   └── VERIFY.md               （阶段 5）
    └── SmartStick/                 ← 阶段 3 由 CubeMX 生成，阶段 4 我往里写代码
        ├── SmartStick.ioc
        ├── Core/
        │   ├── Inc/                （CubeMX 头文件 + user/ 我们的头文件）
        │   └── Src/                （CubeMX 源文件 + user/ 我们的源文件）
        └── MDK-ARM/                （Keil 工程）
```

> 下载的第三方参考资料与压缩包**只保留在本地、不入库**，排除规则见第 9.3 节。

| 文件 | 所属阶段 | 产出方 | 状态 |
|------|----------|--------|------|
| `WORKFLOW.md`（本文） | 0 | 我 | ✅ 已冻结 |
| `demo/docs/PINOUT.md` | 1 | 我 | ✅ 已产出 |
| `demo/docs/CUBEMX_GUIDE.md` | 1 | 我 | ✅ 已产出 |
| `demo/docs/ARCHITECTURE.md` | 2 | 我 | ✅ 已产出 |
| `demo/docs/CODE_STANDARD.md` | 2 | 我 | ✅ 已产出 |
| `demo/SmartStick/`（CubeMX Keil 工程） | 3 | 你 | ⏳ 待执行（照 `CUBEMX_GUIDE.md`） |
| `demo/SmartStick/**/user/`（全部电控代码） | 4 | 我 | ⏳ |
| `demo/docs/VERIFY.md` | 5 | 我 | ⏳ |
| 调试记录与修复迭代 | 6 | 你 + 我 | ⏳ |

---

## 9. Git / 版本管理约定

> 仓库已 clone 完成、`origin` 已配好，不需要再执行 `git init` 或 `git remote add`；命令都在项目根目录 `smart_sti/` 下执行。

- **远程仓库**：<https://github.com/ciolence/SmartStick>（别名 `origin`，分支 `main`）。
- **本地仓库根 = 项目根目录**（本文档所在目录）。
- **原则**：文档与源码入库；编译产物、个人 IDE 配置、第三方参考资料、大压缩包**一律不入库**。v1 阶段两人协作，直接往 `main` 提交，不搞复杂分支模型。

### 9.1 什么时候提交、提交信息怎么写

| 时机 | 提交什么 | 提交信息示例 |
|---|---|---|
| 阶段 1 / 2 / 5 每份文档产出后 | `WORKFLOW.md` + `demo/docs/*.md` | `docs: 补充上电验证 SOP` |
| 阶段 3 CubeMX 生成或改动工程后 | `demo/SmartStick/`（`.ioc` + `Core/` + `MDK-ARM/`） | `chore: CubeMX 重新生成工程（改动：开启 USART3）` |
| 阶段 4 每完成一个驱动 / 业务模块 | 对应 `Core/*/user/` 源码 | `feat: 超声波驱动 + selftest + shell 命令` |
| 修 Bug、调参标定 | 相关文件 | `fix: 修复 I2C 扫描偶发漏设备` |

- 信息前缀统一用：`feat`（新功能 / 驱动）、`fix`（修复）、`docs`（文档）、`refactor`（重构）、`chore`（工程 / 配置 / 构建）。
- **一次提交只做一件事**：CubeMX 重新生成的改动与手写业务代码**分开提交**，出问题好定位、好回退。

### 9.2 常用命令

```powershell
git status                  # 提交前必看：确认没有多余文件混进来
git diff --stat             # 看改了哪些文件、各改了多少
git add -A                  # 暂存全部改动（也可 git add demo/SmartStick 只暂存某个目录）
git commit -m "feat: xxx"   # 提交
git pull --rebase           # 推送前先同步远程（曾在 GitHub 网页上直接改过文件时必做）
git push                    # 推送到 GitHub
git log --oneline -10       # 看最近 10 条提交
```

### 9.3 入库红线

1. **编译产物不进库**：`Objects/`、`Listings/`、`*.axf`、`*.hex`、`*.map`、`*.crf`、`*.d`、`*.uvguix.*`、`*.uvoptx` 等由 `.gitignore` 统一排除。
2. **个人 IDE / 系统文件不进库**：`.vscode/`、`.idea/`、`Thumbs.db`、`desktop.ini` 等。
3. **第三方参考资料与大压缩包不进库**：参考资料目录通过 `.git/info/exclude` 本地排除（该文件本身不会被提交，公开仓库里不会暴露第三方项目信息）；`*.zip / *.rar / *.7z` 由 `.gitignore` 通配排除。
4. **不留敏感信息**：WiFi 密码、云平台 Token、第三方 API Key 不写进源码、不提交；确需本机配置的单独放一个文件并加入 `.gitignore`。
5. **CubeMX 重新生成后**：先 `git status` / `git diff` 核对改动范围，确认 `user/` 目录与 USER CODE 区代码没被吞掉，再提交。
6. **回退**：优先用 `git revert <hash>`（生成一条反向提交，历史可追溯）；`git checkout -- <file>` 会丢弃未提交改动，慎用。

