# 智能盲杖 Demo v1 — 电控开发工作流程

> 本文档是前期讨论的共识记录 + 开发流程约定。
> 后续所有阶段的产出物、验收标准均以本文为准。任何共识变更请回改本文。
>
> **当前进度：阶段 0~3 已完成（需求冻结 / 硬件规划 / 架构规范 / CubeMX 工程）→ 阶段 4（代码实现）进行中。**
> 开放项已于 2026-09-23 逐条确认，见第 6 节；阶段 3 工程审查结论见 5.3 节。
>
> **远程仓库：<https://github.com/ciolence/SmartStick>（分支 `main`）——日常 Git 操作见第 9 节。**

---

## 1. 项目背景

- 做一个面向视障人士出行辅助的**智能导盲杖**（Demo v1）；功能划分与硬件选型参考同类开源导盲杖项目的公开资料。
- 参考项目为「双树莓派 + 双 micro:bit + ESP32」四机架构，我们**裁剪**为 **STM32F103C8T6 单主控**方案。
- 参考资料已就位：`HAVE.md`（已有模块手册）、`HARDWARE.md`（购置清单 / 参考项目代码实证 / 供电 / 引脚建议 / 踩坑清单）。

## 2. 功能冻结清单（Demo v1）

> 优先级定义：**P0 = v1 必须做**；**P1 = v1 做，但可降级**；**P2 = v1 只预留（引脚/接口/代码桩），不实现业务**。

### 2.1 要做的

| # | 功能 | 实现硬件 | 优先级 | 说明 |
|---|------|----------|--------|------|
| 1 | **手扶（自动牵引）** | L298N + 双 GA25-370 DC12V 减速电机 + 从动轮（三轮车底盘） | P0 | 双电机差速实现转向与引导；底盘立一根扶杆，盲人手扶跟随 |
| 2 | **避障** | 超声波模块 ×1（正前方，US-100 类 UART 模式） | P0 | 正前方测距 → 分级减速/停车 + 提示 |
| 3 | **姿态检测** | MPU6050（I²C） | P0 | 跌倒/倾角检测，替代参考项目 micro:bit |
| 4 | **报警** | 有源蜂鸣器 + 振动马达（三极管驱动） | P0 | v1 仅**本地报警**（声/震）；跌倒 SOS 与避障提示均走此通道 |
| 5 | **OLED 状态显示** | 已有的 4 脚 I²C OLED | P0 | 现场显示距离/姿态/电量/错误码，是排错利器（O3：并入） |
| 6 | **电池电压监测** | ADC + 电阻分压 | P1 | 低电量报警并入功能 4（O4：并入，中等优先级） |

### 2.2 v1 只预留、不实现业务（P2）

| # | 项 | 预留方式 | 说明 |
|---|----|----------|------|
| 7 | **蓝牙 UART** | USART3（PB10/PB11）引脚与驱动骨架就位 | O6：**中高优先级**，v1 只预留串口透传 + shell 骨架，不写手机业务 |
| 8 | **QMC5883L 磁力计** | 挂 I²C1 总线（不占新引脚），驱动 + 校准命令就位 | O5：**中等优先级**，v1 应物理安装；只做「能读到数据 + 可校准」，不做航向业务 |
| 9 | **按键** | PA11（SOS）/ PA12（模式）已配置；PB4/PB5 备用位**暂未配置** | O2：低优先级。v1 用 2 键；启用备用键需 CubeMX 重生成一次 |
| 10 | **超声波第二路 / 舵机云台** | PA0/PA1（第二路 Trig/Echo）；PA8（舵机信号，走软件 PWM） | O1：第二路暂不装；O7：转向绕行代码写好但**默认关闭** |
| 11 | **4G/WiFi 联网、GPS、语音** | 不预留引脚（USART3 若被蓝牙占用则需重新分配） | v1 范围外 |

### 2.3 明确不做的（v1 范围外）

- 联网上报 / 4G / WiFi（**预留蓝牙 UART 与手机通信**，手机侧另做软件，后期再启动）
- GPS、语音播报（DFPlayer）、语音识别、BME280、视觉（YOLOv5/车牌）、云服务器
- SG90 舵机（参考项目也未使用；引脚可预留，见 2.2 #10）

## 3. 已达成的共识与决策记录

| 主题 | 决策 |
|------|------|
| 核心板 | STM32F103C8T6（64KB Flash / 20KB RAM，资源紧张是既定约束，编码需注意体积） |
| 开发环境 | CubeMX + Keil MDK5；用户负责 CubeMX 图形化配置，代码放入 `demo/` |
| 调试硬件 | 有 ST-Link 可烧录（SWD 在线调试未接触过，可作为兜底手段，不作为主要排错手段） |
| 工程方法 | 认可：串口调试命令行、模块自测入口 + 上电验证清单、CubeMX 共存规范、错误码/日志规范、参数标定机制 |
| 工程方法 | 否决：PC 端模拟验证（嫌麻烦，不做） |
| 报警路线 | v1 纯本地（声/震）；蓝牙→手机上报为 v2 预留（只留串口，不写业务） |
| 交付目标 | 代码力求一次写对（用户无法即时调试，最终统一上硬件调试），因此**便捷调试是一等需求** |
| **工程命名与路径** | 实际工程在 **`demo/HAL_SMART_STICK/`**，CubeMX 工程名 **`HAL_OLED`**（复用既有 HAL+OLED 模板）。**不改名**——改名会破坏 CubeMX/Keil 的内部引用，收益为零 |
| **代码缩进** | **2 空格**（与用户 Keil5 编辑器设置一致）。见 `CODE_STANDARD.md` 第 9 节 |
| **电源（暂定）** | 正式电源未定；**测试用 11.1V / 1200mAh 3S 锂电**。低电阈值先按 3S（10.5V / 9.9V）、分压比按 33k/10k 写，**换电池只需改参数**；**续航估算延后**（用户要求先做代码） |
| **既有第三方文件** | 工程里已有 `OLED.c/h`+`OLED_Font.h`（**硬件 I2C1**，SSD1306 序列）、`Key.c/h`、`Delay.c/h`。处置见 `ARCHITECTURE.md` 第 5 节 |
| **电机 / 驱动（O8）** | **GA25-370 DC12V 减速电机 ×2 + L298N，开环控制**（推翻原 TT 1:48 方案，**供电树改为 12V 主电源**，见 `demo/docs/PINOUT.md`） |
| **超声波（O1）** | v1 只装 **1 个（正前方）**；第二路**引脚预留**（PA0/PA1）；优先 3.3V 逻辑模块（US-100/AJ-SR04M），驱动同时支持 UART 与 Trig/Echo 两种模式 |
| **按键（O2）** | 预留。主引脚 PA11（SOS）/ PA12（模式）已配；先做 GPIO + 去抖 + shell 读键 |
| **OLED（O3）** | **并入 v1**；用户提供驱动代码（已到位），我方做「适配层 + 显示排版」 |
| **电池电压（O4）** | **并入**；PA4（ADC1_IN4）+ 33kΩ/10kΩ 分压 + 100nF |
| **QMC5883L（O5）** | **预留**，应装机；只挂 I²C + 驱动 + 校准命令，不做航向业务 |
| **蓝牙（O6）** | **中高优先级**，v1 装模块但只做串口透传骨架；USART3（PB10/PB11） |
| **牵引行为（O7）** | v1 = **直行** + 遇障分级（远→减速+提示，近→停车+报警）；**转向/绕行代码写好但默认不启用**，细节到阶段 4 后半段与用户确认 |
| **硬件知识缺口** | 遇到不确定的硬件/驱动细节**先问用户**，不自行假设 |

## 4. 角色分工

| 角色 | 职责 |
|------|------|
| 你 | 确认需求与开放项；按指南配置 CubeMX 并生成 Keil 工程；拿到硬件后按验证清单统一调试、反馈串口日志 |
| 我 | 写全部规划/规范/指南文档；写全部电控代码（驱动层 + 业务层 + 调试设施）；交付验证 SOP；调试期远程排错 |

## 5. 开发流程（阶段 0 ~ 6）

### 阶段 0：需求冻结 ✅ 已完成（2026-09-23）
- 产出：本文档（含第 6 节开放项确认结果）。

### 阶段 1：硬件资源规划 ✅ 已产出
- 产出（`demo/docs/`）：`PINOUT.md`、`CUBEMX_GUIDE.md`。

### 阶段 2：架构与编码规范 ✅ 已产出
- 产出（`demo/docs/`）：`ARCHITECTURE.md`、`CODE_STANDARD.md`。
- 验收标准：你过目认可，后续代码以它为准绳。

### 阶段 3：CubeMX 工程搭建 ✅ 已完成（2026-09-24）
- 产出：`demo/HAL_SMART_STICK/`（`HAL_OLED.ioc` + `Core/` + `Drivers/` + `MDK-ARM/HAL_OLED.uvprojx`），已含用户既有 `OLED.c/h`、`Delay.c/h`、`Key.c/h`。
- Keil 已能编译（产出 `HAL_OLED.axf`）；基线体积：**Flash 9.57 KB / RAM 2.09 KB**（含 HAL + OLED + Delay + Key），余量充足。

#### 5.3 阶段 3 审查记录（2026-09-24，我逐文件核对）

| 检查项 | 结论 |
|--------|------|
| 芯片/封装 | STM32F103C8Tx / LQFP48 ✅ |
| 时钟树 | HSE 8MHz → PLL×9 → SYSCLK 72MHz；APB1 /2、APB2 /1、ADC /6、FLASH_LATENCY_2 ✅ |
| I2C1 | PB6/PB7，**400 kHz Fast Mode**，无 NVIC ✅ |
| USART1 / 2 / 3 | 115200 / 9600 / 9600，8N1，方向 TX+RX ✅；仅 USART1、USART3 使能中断，优先级 5 ✅（USART2 轮询） |
| TIM3 | PSC=3、ARR=999 → **18 kHz**；CH3/CH4 = PWM1、Pulse=0 ✅ |
| TIM4 | PSC=71 → **1 MHz 微秒基准**，ARR=65535 ✅ |
| ADC1 | IN4(PA4)、55.5 cycles、连续转换、软件触发 ✅（F1 无 Resolution / DMA Continuous / Common Settings 项，属正常，见 `CUBEMX_GUIDE.md` 附录） |
| GPIO 初值 | IN1~IN4 / VIB = **LOW**（电机不转、不震）；BUZZ = **HIGH**（静音）；LED_HB = HIGH（熄灭）✅ |
| 空闲脚处理 | PA0/PA1/PA5/PA6/PA7/PA8/PA15、PB2/PB3/PB4/PB5、PC14/PC15 设为模拟输入 ✅ |
| NVIC 分组 | `NVIC_PRIORITYGROUP_4`；SysTick = 15（最低）✅ |
| 工程设置 | MDK-ARM V5.32、**C99**、One ELF Section per Function、`KeepUserCode=true`、`FreePins=true`、Stack 1KB / Heap 0.5KB ✅ |
| **偏差 1** | PB4/PB5（备用按键）未配置 → **采纳**：v1 只用 SOS/MODE 两键，备用键低优先级，需要时再重生成一次 |
| **偏差 2** | PB0/PB1（ENA/ENB）未加 User Label → **无影响**：走 TIM3 通道句柄，不依赖标签 |
| **偏差 3** | 目录名 `HAL_SMART_STICK`、工程名 `HAL_OLED` 与指南原名 `SmartStick` 不一致 → **采纳现状**，文档已按实际改写 |
| **待整改** | `Delay.c` 直接操作 SysTick（会停掉 HAL 1ms 时基）→ 阶段 4 由我改造为 TIM4 基准（见 `ARCHITECTURE.md` 5.2） |

### 阶段 4：代码实现（我做）⏳ 进行中
- 代码位置：`demo/HAL_SMART_STICK/Core/{Inc,Src}/user/**`（CubeMX 不管理的独立目录）。
- 顺序：① 基础框架（`err`/`log`/`cfg`/`sched`/`shell`/`bsp` + `main.c` 钩子）→ ② 驱动层逐模块（每模块含 `selftest` + shell 命令）→ ③ 业务层（避障、跌倒、报警、牵引）→ ④ 整合。
- 每完成一个模块即交付，附「用 shell 如何验证它」。
- **用户参与点**：① OLED 控制器型号确认（SSD1306/SH1106）；② 牵引转向细节（阶段 4 后半，O7）。

### 阶段 5：编译与静态验证（我做）
- Keil 命令行编译（`UV4 -b`）做到 0 Error / 0 Warning。
- 产出 `demo/docs/VERIFY.md` — 上电验证 SOP。

### 阶段 6：上硬件统一调试（你做 + 我支持）
- 按 `VERIFY.md` 逐项执行；异常时把串口日志发我，远程定位修复。

## 6. 开放项确认结果（2026-09-23 冻结）

| # | 问题 | 确认结果 | 决策影响 |
|---|------|----------|----------|
| O1 | 超声波数量/型号 | **1 个（正前方）+ 型号确定为 HC-SR04**；第二路**预留**（PA2/PA3），先做一路 | PA0=TRIG / PA1=ECHO(EXTI1)；ECHO **必须分压**；驱动默认 `CFG_US_MODE=1`(TRIG_ECHO) |
| O2 | 按键数量 | **可预留**，先分配主要引脚；低优先级 | PA11/PA12 已配；PB4/PB5 备用（未配） |
| O3 | OLED 并入 v1 | **并入**；**用现有驱动，不再换型号** | 硬件 I2C1 + SSD1306 序列 → 适配层（探测 + 1KB 帧缓冲 + 按页批量刷） |
| O4 | 电池电压监测 | **并入**，中等优先级 | PA4 = ADC1_IN4，33kΩ/10kΩ + 100nF |
| O5 | QMC5883L | **预留**，中等优先级，**应装** | I2C1（0x0D），只做驱动 + 校准 |
| O6 | 蓝牙模块 | **中高优先级**，可能要装 | USART3（PB10/PB11） |
| O7 | 牵引 v1 行为 | 先直行；复杂牵引代码先写好但不启用；细节到时再问 | `CFG_ADV_GUIDE_EN=0` 默认关 |
| O8 | 电机确认 | **GA25-370 DC12V + L298N，开环** | 供电树改 12V；PWM 18kHz；限速与软启动保护 |
| O9 | `Delay.c` 整改 | **用户同意**（详见 `ARCHITECTURE.md` 5.2） | 改 TIM4 基准，函数签名不变；修复 SysTick 被停导致的 HAL 时基失效 |
| O10 | Keil 工程结构 | **授权我自行调整 `.uvprojx`**（补 `user/` 分组与 include 路径） | 阶段 4-B 交付一键同步脚本，CubeMX 重生成后可恢复 |

> 阶段 4 期间仍需澄清、**届时问用户、不自行假设**：
> 1. **HC-SR04 到货实测**（触发灵敏度 / 远距离稳定性）；若 ECHO 无反应，先查 5V 供电与 2.2kΩ/3.3kΩ 分压。
> 2. 正式电源形态（3S 锂电组 / 12V 铅酸 / 适配器）与续航目标 → 决定低电阈值与**后续续航估算**（用户明确：先做代码，续航留到后面算）。
> 3. 是否最终装第二路超声波（PA2/PA3，需关闭 USART2）→ 决定是否解禁转向绕行。
> 4. 正式电源定案后是否需要参数掉电保存（`CFG_ENABLE_NV_SAVE`）。

## 7. 调试策略（为什么能"一次写对"）

1. **串口 shell**：电脑发文本命令即可单独驱动任意执行器、读任意传感器、改参数、跑校准——把"在线调试"降级为"在线人肉测试"，不依赖 SWD 技能。
2. **模块自测入口**：每个驱动自带 `selftest`，配合 shell 逐模块验证。
3. **验证 SOP**：`VERIFY.md` 让统一调试变成按单执行。
4. **错误码 + 日志分级**：故障自报家门，而非"没反应"。
5. **参数集中 + shell 在线调参**：阈值、标定值不发版即可修改。
6. **CubeMX 共存规范**：重新生成代码不吞我们的代码。
7. 兜底：ST-Link 在线调试。

## 8. 交付物清单与目录结构

```
smart_sti/                          ← Git 仓库根（远程：github.com/ciolence/SmartStick）
├── README.md                       （仓库首页简介）
├── .gitignore                      （编译产物 / 临时文件排除规则）
├── HARDWARE.md / HAVE.md           （前期资料，不动）
├── WORKFLOW.md                     （本文：总纲 / 进度追踪）
└── demo/
    ├── docs/                       ← 我产出的全部文档
    │   ├── PINOUT.md               （阶段 1）
    │   ├── CUBEMX_GUIDE.md         （阶段 1）
    │   ├── ARCHITECTURE.md         （阶段 2）
    │   ├── CODE_STANDARD.md        （阶段 2）
    │   └── VERIFY.md               （阶段 5）
    └── HAL_SMART_STICK/            ← CubeMX + Keil 工程（工程名 HAL_OLED）
        ├── HAL_OLED.ioc            （CubeMX 配置，用户维护）
        ├── Core/
        │   ├── Inc/                （CubeMX 头文件 + 用户既有 OLED.h/OLED_Font.h/Delay.h/Key.h）
        │   │   └── user/           ★ 阶段 4：我们的全部头文件（CubeMX 不管）
        │   └── Src/                （CubeMX 源文件 + 用户既有 OLED.c/Delay.c/Key.c）
        │       └── user/           ★ 阶段 4：我们的全部源文件（CubeMX 不管）
        ├── Drivers/                （STM32 HAL + CMSIS，CubeMX 管理）
        └── MDK-ARM/HAL_OLED.uvprojx（Keil 工程）
```

> 下载的第三方参考资料与压缩包**只保留在本地、不入库**，排除规则见第 9.3 节。

| 文件 | 所属阶段 | 产出方 | 状态 |
|------|----------|--------|------|
| `WORKFLOW.md`（本文） | 0 | 我 | ✅ 已冻结 |
| `demo/docs/PINOUT.md` | 1 | 我 | ✅ 已产出 |
| `demo/docs/CUBEMX_GUIDE.md` | 1 | 我 | ✅ 已产出（含实测附录） |
| `demo/docs/ARCHITECTURE.md` | 2 | 我 | ✅ 已产出 |
| `demo/docs/CODE_STANDARD.md` | 2 | 我 | ✅ 已产出 |
| `demo/HAL_SMART_STICK/`（CubeMX Keil 工程） | 3 | 你 | ✅ 已完成并审查 |
| `demo/HAL_SMART_STICK/Core/*/user/`（全部电控代码） | 4 | 我 | ⏳ 进行中 |
| `demo/docs/VERIFY.md` | 5 | 我 | ⏳ |
| 调试记录与修复迭代 | 6 | 你 + 我 | ⏳ |

---

## 9. Git / 版本管理约定

> 仓库已 clone 完成、`origin` 已配好，不需要再执行 `git init` 或 `git remote add`；命令都在项目根目录 `smart_sti/` 下执行。

- **远程仓库**：<https://github.com/ciolence/SmartStick>（别名 `origin`，分支 `main`）。
- **本地仓库根 = 项目根目录**（本文档所在目录）。
- **原则**：文档与源码入库；编译产物、个人 IDE 配置、第三方参考资料、大压缩包**一律不入库**。v1 阶段两人协作，直接往 `main` 提交，不搞复杂分支模型。

### 9.1 什么时候提交、提交信息怎么写

| 时机 | 提交什么 | 提交信息示例 |
|---|---|---|
| 阶段 1 / 2 / 5 每份文档产出后 | `WORKFLOW.md` + `demo/docs/*.md` | `docs: 补充上电验证 SOP` |
| 阶段 3 CubeMX 生成或改动工程后 | `demo/HAL_SMART_STICK/`（`.ioc` + `Core/` + `MDK-ARM/*.uvprojx`） | `chore: CubeMX 重新生成工程（改动：开启 USART3）` |
| 阶段 4 每完成一个驱动 / 业务模块 | 对应 `Core/*/user/` 源码 | `feat: 超声波驱动 + selftest + shell 命令` |
| 修 Bug、调参标定 | 相关文件 | `fix: 修复 I2C 扫描偶发漏设备` |

- 信息前缀统一用：`feat`（新功能 / 驱动）、`fix`（修复）、`docs`（文档）、`refactor`（重构）、`chore`（工程 / 配置 / 构建）。
- **一次提交只做一件事**：CubeMX 重新生成的改动与手写业务代码**分开提交**，出问题好定位、好回退。

### 9.2 常用命令

```powershell
git status                  # 提交前必看：确认没有多余文件混进来
git diff --stat             # 看改了哪些文件、各改了多少
git add -A                  # 暂存全部改动（也可 git add demo/HAL_SMART_STICK 只暂存某个目录）
git commit -m "feat: xxx"   # 提交
git pull --rebase           # 推送前先同步远程（曾在 GitHub 网页上直接改过文件时必做）
git push                    # 推送到 GitHub
git log --oneline -10       # 看最近 10 条提交
```

### 9.3 入库红线

1. **编译产物不进库**：`Objects/`、`Listings/`、`DebugConfig/`、`RTE/`、`*.axf`、`*.hex`、`*.map`、`*.crf`、`*.d`、`*.o`、`*.uvguix.*`、`*.uvoptx` 等由 `.gitignore` 统一排除（2026-09-24 已用 `git add -A -n` 干跑验证：105 个待入库文件全部是源码/工程文件）。
2. **个人 IDE / 系统文件不进库**：`.vscode/`、`.idea/`、`Thumbs.db`、`desktop.ini` 等。
3. **第三方参考资料与大压缩包不进库**：参考资料目录通过 `.git/info/exclude` 本地排除；`*.zip / *.rar / *.7z` 由 `.gitignore` 通配排除。
4. **不留敏感信息**：WiFi 密码、云平台 Token、第三方 API Key 不写进源码、不提交。
5. **CubeMX 重新生成后**：先 `git status` / `git diff` 核对改动范围，确认 `user/` 目录与 USER CODE 区代码没被吞掉，再提交。
   - ⚠️ 已知风险：CubeMX 重新生成 `*.uvprojx` 时，**手工在 Keil 里加的文件分组可能丢失**（现有 `Hardware`（OLED/Key）与 `System`（Delay）两组就是手工加的）。重生成后若 Keil 里报找不到文件，告诉我，我用脚本把分组重新注入 `.uvprojx`。
6. **回退**：优先用 `git revert <hash>`（生成一条反向提交，历史可追溯）；`git checkout -- <file>` 会丢弃未提交改动，慎用。
