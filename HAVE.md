先说一个型号问题：**没有 QMLC5883L 这个标准型号**，你说的应该是 **QMC5883L**（QST 上海矽睿出的三轴磁阻传感器，GY-271/GY-273 模块上的丝印常被误看成多一个 L）。它和霍尼韦尔 HMC5883L 引脚兼容但**寄存器完全不通用**，这是最常见的踩坑点。下面按这个来写。

---

## 1. 4引脚 OLED（0.96"/1.3"，SSD1306 或 SH1106）

| 引脚 | 功能 | 说明 |
|---|---|---|
| GND | 电源地 | 必须与主控共地 |
| VCC/VDD | 电源正 | **3.3V～5V**，模块内置 LDO 与升压电路 |
| SCL/SCK | I²C 时钟 | 模块已板上拉到 3.3V |
| SDA | I²C 数据 | 同上 |

- **通信**：I²C，7位从机地址默认 **0x3C**（改电阻可切 0x3D），8位写 0x78 / 读 0x79。速率标准 100kHz / 快速 400kHz。
- **驱动**：硬件 I²C 或任意 GPIO 软件模拟都行。SSD1306 控制字节格式为 `[Co][D/C#][000000]`，用来区分"命令/显存数据"。
- **注意**：1.3 寸多为 **SH1106**，显存是 132×64，显示时需**列偏移 2**，直接用 SSD1306 库会左右错位。
- 上电后需发初始化命令序列（关显示→设时钟/复用比→电荷泵 0x8D/0x14→开显示），不是接上就亮。

## 2. L298N 双 H 桥驱动模块

底部排针（从左到右）：**VCC(电机电源) / GND / 5V / ENA / IN1 / IN2 / IN3 / IN4 / ENB**，两侧螺丝端子为 OUT1-OUT2（电机A）、OUT3-OUT4（电机B）。

- **电压**：电机驱动电压 **5V～35V**（芯片极限 46V）；逻辑电压 5V（VSS 4.5～7V）。
- **电流**：每通道持续 2A、峰值 3A；**H 桥压降约 2V**，所以 12V 供电到电机只剩约 10V，选型时要把这个算进去。
- **逻辑电平**：TTL，高 ≥2.3V、低 ≤1.5V —— 3.3V 的 ESP32/STM32 可以直接驱动。
- **信号真值表**（ENA/IN1/IN2）：

| ENA | IN1 | IN2 | 状态 |
|---|---|---|---|
| 0 | X | X | 停止（滑行） |
| 1 | 0 | 0 | 刹车 |
| 1 | 0 | 1 | 正转 |
| 1 | 1 | 0 | 反转 |
| 1 | 1 | 1 | 刹车 |

- **驱动方式**：IN1/IN2 定方向，**ENA 接 PWM 调速（必须取下跳线帽）**；不调速时跳线帽把 ENA 短到 5V = 全速。
- **5V 跳线帽**：供电 ≤12V 时保留，板载 78M05 输出 5V 可反向给单片机供电；**供电 >12V 必须拆掉跳线，并从外部给 5V 脚供逻辑电**，否则稳压器严重发热。

## 3. 直流电机（有刷，两引脚）

| 引脚 | 功能 |
|---|---|
| 红线/端子A | 绕组一端 |
| 黑线/端子B | 绕组另一端 |

- **无极性之分**：两根线对调 = 反转，所以换向必须由 H 桥（L298N 等）完成，MCU 的 GPIO **不能直接驱动电机**（电流不够，且会烧 IO）。
- **驱动方式**：H 桥换向 + **PWM 调速**（频率建议 10kHz～20kHz，避开人耳听觉范围）。PWM 加在 ENA 上，IN1/IN2 保持一高一低。
- **电气要点**：启动/堵转电流可达额定值的 3～5 倍，电源要留余量；并在电机两端并联 **100nF 陶瓷电容**、电机壳与电源间加 100µF 电解，抑制电刷火花干扰（否则 I²C 传感器和单片机会莫名复位）。
- 常见 TT 减速马达典型值：3～6V（常用 6V），减速比 1:48，空载约 100～200mA、堵转约 1A 左右。具体以你手上的型号铭牌为准。

## 4. SG90 舵机（9g 微型舵机）

| 线色 | 功能 | 电气 |
|---|---|---|
| 棕/黑 | GND | 0V |
| 红 | VCC | **4.8V～6.0V（典型 5V）** |
| 橙/黄 | 信号 PWM | 3.3V/5V 逻辑电平 |

- **通信**：无协议，就是一路 **50Hz（周期 20ms）** 的 PWM，用**高电平脉宽**编码角度：

| 高电平 | 角度 | 占空比 |
|---|---|---|
| 0.5ms | 0° | 2.5% |
| 1.0ms | 45° | 5.0% |
| 1.5ms | 90°（中位） | 7.5% |
| 2.0ms | 135° | 10.0% |
| 2.5ms | 180° | 12.5% |

- **实用建议**：不同批次线性度有偏差，**先用 1.0～2.0ms 这个安全区间**再实测标定到全量程；堵到机械极限会持续拉大电流，别让程序输出超范围脉宽。
- **供电必须独立**：运行电流 100～250mA、堵转更高，**多个舵机绝不能挂单片机板载 5V**，外接 5V 但要**与 MCU 共地**；信号端 3.3V 主控（ESP32）一般能识别，不放心就加电平转换。

## 5. MPU6050 六轴（3轴加速度 + 3轴陀螺仪）

| 引脚 | 功能 | 说明 |
|---|---|---|
| VCC | 电源 | 模块 3.3～5V（板载 LDO 降到 3.3V）；**裸片必须 3.3V** |
| GND | 地 | 共地 |
| SCL / SDA | I²C 从机接口 | 模块已带 4.7kΩ 上拉 |
| XCL / XDA | I²C **主机**接口 | 用来外接磁力计（正好可挂 QMC5883L），配合 DMP 输出 9 轴融合 |
| AD0 | 地址选择 | 接地/悬空 = **0x68**，接 VCC = **0x69** |
| INT | 中断输出 | 数据就绪、自由落体、运动检测等 |

- **通信**：I²C，最高 400kHz。7位地址 0x68 → 实际发送字节 **写 0xD0 / 读 0xD1**（地址左移 1 位 + 读写位），很多例程写 0xD0 就是这个原因。
- **驱动**：上电后先向 **0x6B 写 0x80 复位**，再写 0x00 唤醒、选时钟源（通常用 X 轴陀螺 PLL）；读 **0x75 WHO_AM_I** 应返回 0x68，用来确认通信是否正常。
- **量程**：加速度 ±2/4/8/16g，陀螺 ±250/500/1000/2000 °/s，16 位 ADC。原始值需除以灵敏度系数（如 ±2g 时 16384 LSB/g）。
- 芯片自带 DMP 可直接输出四元数，省掉自己做卡尔曼/互补滤波。

## 6. QMC5883L 三轴磁罗盘（GY-271 / GY-273）

| 引脚 | 功能 | 说明 |
|---|---|---|
| VCC | 电源 | **模块 3.3～5V**（板载稳压+电平转换）；裸片 2.16～3.6V，别接 5V |
| GND | 地 | |
| SCL / SDA | I²C | 需 4.7kΩ 上拉，模块一般已内置 |
| DRDY | 数据就绪中断 | 可选，>100Hz 高速读取时用，默认内部上拉、低电平有效约 250µs |

- **通信**：I²C，7位地址 **0x0D**（注意 HMC5883L 是 0x1E，所以第一步永远是跑 **I²C 扫描**确认芯片）。8 位写地址 0x1A / 读 0x1B。速率 100/400kHz。
- **驱动**：向 **控制寄存器 0x0B 写 0x01** 进入连续测量模式；从 **0x00 起连续读 6 字节**得到 X/Y/Z 各 16 位（小端）。状态寄存器 0x09 可查 DRDY 与 DOR（数据溢出/丢帧）。
- **参数**：量程 ±2G / ±8G（默认 2G，强磁场附近要切 8G 否则饱和），ODR 10/50/100/200Hz，16 位输出，航向精度 1°～2°，工作电流约 75～100µA。
- **关键**：**必须做硬磁/软磁校准**（原地缓慢转 360° 采集 X/Y 极值求偏移与比例），并加**磁偏角**修正才能得到真北；安装要远离电机、喇叭、导线和铁质螺丝——哪怕一颗钢螺丝都能把读数带偏。倾斜时航向会失真，需配合 MPU6050 做倾角补偿。

---

## 汇总速查

| 模块 | 供电 | 通信/信号 | 关键参数 |
|---|---|---|---|
| 4脚 OLED | 3.3～5V | I²C @0x3C | SSD1306/SH1106，128×64 |
| L298N | 电机 5～35V / 逻辑 5V | INx 电平 + ENA/ENB PWM | 2A/通道，压降约 2V |
| 直流电机 | 按额定（常见 3～12V） | H 桥换向 + PWM 10～20kHz | 堵转电流大，需续流与滤波 |
| SG90 | 4.8～6V | 50Hz PWM，0.5～2.5ms | 0～180°，1.8kg·cm |
| MPU6050 | 3.3～5V | I²C @0x68/0x69，400kHz | ±16g / ±2000°/s，带 DMP |
| QMC5883L | 3.3～5V | I²C @0x0D | ±2/8G，200Hz，需校准 |

**几个通用铁律**：所有模块**必须共地**；电机/舵机走独立电源，别和控制电路抢 5V；I²C 上拉电阻只留一组（模块自带的就够了，多组并联会导致总线拉不下来）；OLED、MPU6050、QMC5883L 三个 I²C 设备地址不冲突，可以放心挂同一条总线。

需要的话我可以接着给你画一份**接到 Arduino/ESP32/STM32 的完整接线图**，或者直接给出这六个模块的**驱动代码框架**。