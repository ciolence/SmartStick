# HARDWARE.md — 智能盲杖（Demo v1 裁剪方案）硬件清单

> 本文基于对参考项目（同类开源导盲杖）全部代码与其演示文档硬件页的实证分析，结合你 `HAVE.md` 中已有的器件，
> 给出「**保留基础功能、裁掉高阶功能**」路线下需要补充的硬件。
>
> 带 ⚠️ 的是我无法 100% 确认的细节（型号/电平/极性），请上网复核后再下单。

---

## 一、参考项目到底用了什么（代码实证，不是猜的）

| 功能 | 参考实现硬件 | 参考依据 |
|---|---|---|
| 跌倒检测 | **micro:bit ×1**（内置加速度计+倾角） | micro:bit 端跌倒检测脚本 |
| 无线转发 | **micro:bit ×1**（radio 收包 → USB 串口） | micro:bit 端无线转发脚本 |
| 主控/视觉/语音（A机） | **树莓派（Pi4B）+ CSI/USB 摄像头 + 麦克风 + 蓝牙耳机** | 树莓派 A 机主控脚本 + 视觉识别目录 |
| 服务器/定位/气象（B机） | **树莓派 + USB 摄像头 + NEO-6M GPS + BME280 + 超声波** | 树莓派 B 机 Flask 服务 + 气象 / 测距脚本 |
| 自动牵引 | **ESP32 + L298N + 直流电机 ×2** | ESP32 端电机驱动代码 |
| 云端 | 阿里云/腾讯云 ECS + Flask + React 前端 | 云端服务端目录 |
| 避障 | **HC-SR04 类超声波**（Trig=BCM19, Echo=BCM26） | 树莓派端测距脚本 |
| 气象 | **BME280**（I2C 0x77） | 树莓派端气象脚本 |
| 定位 | **NEO-6M GPS**（串口 `/dev/ttyAMA0` @9600） | 树莓派端 GPS 测试脚本 |
| 车牌识别 | 摄像头 + 百度云 OCR（**硬编码的 API Key 已泄露/大概率失效，别用**） | 车牌识别脚本 |
| 视觉识别 | YOLOv5s（`yolov5s.pt`）+ OpenCV Hough 变换 | 视觉识别目录 |

**结论：参考项目是「双树莓派 + 双 micro:bit + ESP32」四机架构**，你手上的 STM32F103C8T6 要一肩挑，所以必须裁剪。

---

## 二、你已有的家底 & 在方案里的用法

| 你有的 | 在新方案里的角色 | 说明 |
|---|---|---|
| STM32F103C8T6 | **唯一主控** | 替代 ESP32 + micro:bit + 大部分树莓派职责 |
| 4脚 OLED（I2C） | 状态显示 / 菜单 | 显示避障距离、电量、联网状态、告警 |
| L298N | 自动牵引 | 保留（注意 ~2V 压降） |
| 直流电机 ×N | 自动牵引执行 | 保留 |
| SG90 舵机 | 可选：摆头扫描 / 传感器云台 | ⚠️ **参考项目根本没用到舵机**，属于你的加分项，可不用 |
| MPU6050 | **跌倒检测**（替代 micro:bit）+ 倾角补偿 | 你的方案里最关键的替代品 |
| QMC5883L | 航向/转向判定 | 参考项目**没有**磁力计，属于加分项 |

---

## 三、功能取舍表（建议）

| # | 功能 | 决策 | 理由 |
|---|---|---|---|
| 1 | 超声波避障 + 震动/蜂鸣提示 | ✅ **保留（核心）** | 盲杖第一刚需，代码可移植 |
| 2 | 跌倒检测 + 一键 SOS | ✅ **保留（核心）** | MPU6050 完全可替代 micro:bit |
| 3 | 联网上报（位置/告警）+ 手机或网页查看 | ✅ **保留（核心）** | 参考项目 IoT 骨架，必须有网 |
| 4 | 语音播报提示（"前方障碍""已发送求助"） | ✅ **保留（简化）** | 预录语音代替在线 TTS |
| 5 | 自动牵引（电机驱动） | ✅ **保留** | 器件已全，工作量小 |
| 6 | GPS 定位 / 简单航向引导 | 🟡 **保留（简化）** | 只上报坐标，不做路径规划 |
| 7 | 环境温湿度气压显示 | 🟡 **可留可裁** | 想留就加 BME280 |
| 8 | 离线语音命令词（"导航""求救"） | 🟡 **可选** | 用离线识别模块，取代 Google ASR |
| 9 | 红绿灯 / 斑马线 YOLOv5 视觉 | ❌ **裁剪** | STM32 跑不了；可选搬到电脑上跑 |
| 10 | 车牌识别（打车） | ❌ **裁剪** | 依赖百度云 OCR 与摄像头 |
| 11 | 天气 ML 预测（随机森林/回归） | ❌ **裁剪** | 原代码数据来自爬取，实际意义弱 |
| 12 | 双树莓派 + 云 ECS + React 后台 | ❌ **裁剪/本机化** | 用你的电脑跑 Flask 即可 |

---

## 四、**需要补充的硬件清单**

### A. 必买（不买做不了）

| # | 器件 | 关键规格要求 | 数量 | 用途 |
|---|---|---|---|---|
| A1 | **超声波测距模块** | 推荐 **US-100** 或 **AJ-SR04M**（3.3V 逻辑、UART/触发双模式）；若买最常见 **HC-SR04** 则需 5V 供电 + ECHO 分压（1kΩ 串 + 2kΩ 下拉） | 1~2 | 避障（核心） |
| A2 | **有源蜂鸣器模块** | 3.3V 兼容，低电平触发优先 | 1 | 声音提示 |
| A3 | **扁平振动马达**（手机振动马达） | 3V~5V 直流 | 1~2 | 触觉提示（盲杖比蜂鸣器更实用） |
| A4 | 马达驱动小件 | NPN 三极管 S8050 + 1kΩ 基极电阻 + 1N4148 续流二极管；或直接买「振动马达驱动模块」 | 1套 | 驱动 A3（IO 不能直驱） |
| A5 | **无线联网模块**（三选一） | 见下方 ⬇️ | 1 | 上报位置/告警 |
| A6 | **2S 锂电池供电套件** | 2×18650 电池盒（带 BMS 保护板）或成品 7.4V 锂电组 + **8.4V 充电器** | 1套 | 主电源 |
| A7 | **DC-DC 降压模块** | MP1584EN / LM2596S 可调降压，输入 ≥8.4V，输出 5V ≥2A | 1~2 | 给控制电路供电 |
| A8 | 电源开关 + 保险丝座 | 带锁船型/拨动开关；5A 保险丝座 | 各1 | 安全 |
| A9 | 电容 | 100nF 陶瓷 ×2、100µF/25V 电解 ×2 | — | 电机消噪（**不加会莫名复位**） |
| A10 | **ST-Link V2 下载器** | 兼容 STM32F103，SWD 4 线 | 1 | 烧录/调试（若已有可跳过） |
| A11 | **USB 转 TTL 串口模块** | CH340 / CP2102 | 1 | 调 WiFi 模块 AT、读 GPS、串口调试 |
| A12 | 杜邦线 / 面包板或洞洞板 | 母母、公公、母公各一把；洞洞板 + 2.54 排针 + 焊锡 | 1套 | 搭机 |
| A13 | 轻触按键 | 6×6×5mm，配 10kΩ 上拉（或用内部上拉） | 3~4 | SOS / 模式 / 音量 |

**A5 无线联网模块三选一：**

| 方案 | 器件 | 优点 | 缺点 |
|---|---|---|---|
| A | **ESP-01S（ESP8266）** + 独立 AMS1117-3.3 稳压模块 | 最便宜（~10 元），AT 指令串口即用 | 必须稳定 3.3V（峰值 300mA，接 5V 会烧）；只能室内 WiFi |
| B | **ESP32-C3 / ESP32-WROOM 开发板** ⭐推荐 | 好焊好用，可当联网协处理器，也可直接跑传感器 | 稍贵（~20 元） |
| C | **合宙 Air780E / ML307R / EC800M 等 Cat.1 4G 模块** + 物联网卡 | 室外无 WiFi 也能用，盲杖真正形态 | 贵（模块 ~30 元 + 卡费）；⚠️ 别买 SIM800C（2G 已逐步退网） |

> 基础验证阶段先用 A 或 B 就行；想做成"能带出门"的版本，最终要上 C。

### B. 强烈建议买（对应参考项目核心功能）

| # | 器件 | 关键规格要求 | 数量 | 对应参考功能 |
|---|---|---|---|---|
| B1 | **GPS 模块** | 推荐 **ATGM336H**（北斗+GPS，3.3~5V，串口 9600）；或 NEO-6M/NEO-7M（GY-NEO6MV2） | 1 | 实时定位上报 |
| B2 | **串口 MP3 语音模块** | **DFPlayer Mini** + 8Ω/2W 小喇叭 + TF 卡（放预录语音） | 1套 | 语音播报（替代 gTTS+蓝牙耳机） |
| B2' | 或者 **中文 TTS 模块** | SYN6288（串口，文本直接转语音） | 1 | 同上，可动态说话 |
| B3 | **离线语音识别模块** | LU-ASR01 / ASRPRO（天问Block 编程）/ 亚博语音模块 | 1 | 语音助手（替代 Google ASR） |
| B4 | **BME280 模块** | I2C，3.3V，地址 0x76/0x77；⚠️ **别买成 BMP280**（没有湿度） | 1 | 环境检测 |
| B5 | 喇叭/耳机接口件 | 3.5mm 音频座 或 直接焊喇叭 | 1 | 语音输出 |

### C. 可选（想更接近参考项目 / 更好用）

| # | 器件 | 说明 |
|---|---|---|
| C1 | **USB 摄像头（UVC 免驱）** | ⭐性价比最高：把参考项目的 YOLOv5 代码**搬到你电脑上跑**，电脑通过串口/WiFi 通知 STM32 播报。零算力压力，视觉功能也能演示 |
| C2 | **ESP32-CAM（OV2640）** | 想在杖上做简单颜色识别/图传就买；⚠️ 跑不动 YOLOv5，只能做色块检测 |
| C3 | **蓝牙串口模块** HC-05 / JDY-31 | 手机直接当上位机：手机上顺便承担 GPS、语音识别、4G 上网、地图导航 |
| C4 | **RTC 时钟模块 PCF8563** | ⚠️ 要选 PCF8563（I2C 0x51）；**不要买 DS3231/DS1307**（地址 0x68 会和 MPU6050 冲突） |
| C5 | MicroSD 卡模块（SPI） | 本地记录轨迹/日志，掉网不丢数据 |
| C6 | INA219 或电阻分压 | 电池电量监测（分压：2×100kΩ + 100nF 接 ADC 最省钱） |
| C7 | 结构件 | 盲杖本体（可伸缩登山杖/铝合金管）、3D 打印支架、亚克力板、尼龙扎带、魔术贴、热缩管 |

---

## 五、供电方案（关键，别搞错）

参考项目电机是 **TT 减速马达（典型额定 3~6V）**，而 L298N 有 **约 2V 压降**，所以：

```
2S 18650（7.4V 标称 / 8.4V 满电）
   │
   ├─→ L298N 电机电源(VCC)  → 电机实际约 5.4~6.4V ✅ 正好匹配 TT 马达
   │
   └─→ 降压模块(MP1584/LM2596) → 5V ─┬→ STM32 板 VIN(5V) → 板载 AMS1117 → 3.3V
                                     ├→ DFPlayer / 蜂鸣器 / 舵机(SG90 若用)
                                     └→ AMS1117-3.3 二次降压 → ESP-01S(若用方案A)
```

要点：
- ⚠️ **不要用 12V 供电**：L298N 在 >12V 时必须拆 5V 跳线帽并外供逻辑电，且 TT 马达会超压。
- ⚠️ **电机、舵机绝不从 STM32 板载 3.3V/5V 取电**，会拉崩 MCU。
- ✅ **所有模块必须共地**，大电流地线单独走，星形接到电池负极。
- ✅ 每个电机端子并 **100nF 陶瓷**，电源入口并 **100µF 电解**；电机线远离 I2C/超声波信号线。
- 2S 电池**必须配 BMS 保护板 + 8.4V 专用充电器**，别用 5V 直接充、别裸充。

---

## 六、STM32F103C8T6 资源评估 & 建议引脚分配

**资源：** 64KB Flash / 20KB RAM，2×I2C、3×USART、2×SPI、TIM1~TIM4。

**引脚分配（一种可行方案，可自行调整）：**

| 外设 | 引脚 | 备注 |
|---|---|---|
| I2C1：OLED(0x3C) + MPU6050(0x68) + QMC5883L(0x0D) + BME280(0x76) | PB6=SCL, PB7=SDA | 地址不冲突，同挂一条总线 |
| ESP8266 / 4G 模块（AT） | PA9=TX1, PA10=RX1 | 115200 |
| GPS（NMEA） | PA2=TX2, PA3=RX2 | 9600 |
| DFPlayer / 语音识别模块 | PB10=TX3, PB11=RX3 | 二选一或分时复用 |
| 避障超声波 | PA0=TRIG, PA1=ECHO(TIM2_CH2 输入捕获) | ECHO 记得分压 |
| L298N ENA / ENB (PWM) | PB0(TIM3_CH3) / PB1(TIM3_CH4) | 10~20kHz 调速 |
| L298N IN1~IN4 | PB12 / PB13 / PB14 / PB15 | 普通 GPIO |
| 蜂鸣器 / 振动马达 | PB9 / PB8 | 经三极管驱动 |
| 按键 SOS / 模式 | PA11 / PA12 | 内部上拉 |
| 电池电压检测 | PA4 (ADC) | 2×100kΩ 分压 + 100nF |
| 舵机 SG90（可选） | PB8（若不做振动反馈）或软件 PWM | 50Hz |
| SWD 调试 | PA13=SWDIO, PA14=SWCLK | 留给 ST-Link |

**风险提示：**
- 64KB Flash 要装 `MPU6050 DMP 库 + u8g2 + AT 驱动 + NMEA 解析 + MP3 控制` 会**很紧张**。
  → 建议：OLED 用轻量驱动（不用整包 u8g2），或跌倒检测自己写阈值/互补滤波（不上 DMP）；
  → 实在不够，直接换 **STM32F103CBT6（128KB，引脚完全兼容）**，成本 +5 元左右。
- ⚠️ 5V 模块接 3.3V 引脚前请核对：`HC-SR04 的 ECHO`、`SYN6288 的 TX` 等可能是 5V 输出，稳妥做法是分压或选 3.3V 版本模块。

---

## 七、明确不用买的（已裁掉）

- ❌ 树莓派 ×2（改用「电脑 + STM32」）
- ❌ micro:bit ×2（MPU6050 全面替代）
- ❌ 云服务器 / ECS（本地电脑跑 Flask 即可，参考项目的服务端可直接复用）
- ❌ 蓝牙耳机（DFPlayer + 小喇叭更简单；参考项目里写死的声卡编号就是它的坑）
- ❌ 第二套摄像头 + 百度的 OCR API（车牌功能整块砍掉；**代码里硬编码的 APP_ID/KEY 请勿使用**）
- ⚠️ 天气功能如果裁掉，BME280（B4）也可以不买

---

## 八、踩坑清单（血泪向）

1. **电机电刷噪声导致 MCU 复位**：必须加 100nF + 100µF，且电机电源线与 I2C 线分开走。
2. **QMC5883L 不是 HMC5883L**：寄存器完全不同（0x0D vs 0x1E），写代码前先跑 I2C 扫描。
3. **磁力计必须校准 + 远离电机/喇叭/铁螺丝**：装到杖身顶端，原地转 360° 采集极值做硬磁校准。
4. **ESP-01S 接 5V 必烧**；它的电流尖峰会拖垮共用 3.3V，务必单独稳压 + 并 100µF。
5. **I2C 上拉只留一组**：4 个模块各自带 4.7kΩ 并联≈1.2kΩ 一般还能跑，但如果通信不稳，就拆掉多余的。
6. **GPS 室内收不到星**：测试必须到窗边/室外，冷启动要几分钟。
7. **超声波对软质/斜面反射差**：测地面、草丛会读不到，避障建议只测正前方 0.3~3m。
8. **L298N 会发热**：不要堵转太久；`moveForward()` 里那种 `delay()` 阻塞写法别照抄（参考实现里还有看门狗复位的坑，原作者自己在演示文档里也吐槽了）。
9. **超声波 3.3V 供电选型**：HC-SR04 需要 5V 才能工作（至少 4.5V），别接 3.3V。

---

## 九、最小可用集 & 建议搭建顺序

**最小可用集（只做「避障 + 跌倒告警 + 联网上报 + 语音提示」）：**
A1、A2、A3、A4、A5、A6、A7、A8、A9、A10、A11、A12、A13、B2 → **约 150~250 元**（不含电池和已有器件，以实际售价为准）

**搭建顺序（每步都可独立验证，别一次全接）：**
1. STM32 + ST-Link 点灯 + OLED 显示 + 按键读取
2. 挂 I2C 三件套（MPU6050 / QMC5883L）→ 读出数据
3. 超声波 + 蜂鸣器 + 振动马达 → **第一步真正的"盲杖"功能：靠近就震**
4. L298N + 电机 + PWM 调速 → 牵引动作
5. ESP8266/ESP32 联网 → 数据 POST 到电脑上的 Flask，网页能看到
6. GPS → 上报坐标；DFPlayer → 播报语音
7. BME280 / 摄像头视觉（可选）

---

### 附：型号速查（可直接拿去搜）

```
US-100 或 AJ-SR04M 超声波（3.3V 版）/ HC-SR04 + 分压
有源蜂鸣器模块 3.3V
扁平振动马达 3V 5V + S8050 + 1N4148
ESP-01S / ESP32-C3 开发板 / 合宙 Air780E
18650 电池盒 2S 带保护板 + 8.4V 充电器
MP1584EN 降压模块 / LM2596S
ST-Link V2 / CH340 USB转TTL
ATGM336H 或 NEO-6M GPS 模块
DFPlayer Mini + 8Ω 2W 喇叭 + TF卡
LU-ASR01 / ASRPRO 离线语音识别模块
BME280 温湿度气压模块（不要 BMP280）
PCF8563 RTC 模块（不要 DS3231）
```
